apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: controlplane
  namespace: consumer
spec:
  rules:
    - host: cp.consumer.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: controlplane
                port:
                  number: 8081
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dataplane
  namespace: consumer
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: consumer-strip-public@kubernetescrd
spec:
  rules:
    - host: dp.consumer.localhost
      http:
        paths:
          - path: /public
            pathType: Prefix
            backend:
              service:
                name: dataplane
                port:
                  number: 11002
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-public
  namespace: consumer
spec:
  stripPrefix:
    prefixes:
      - /public
