meta {
  name: Request Catalog
  type: http
  seq: 1
}

post {
  url: {{CONSUMER_CP}}/api/mgmt/v4/catalog/request
  body: json
  auth: inherit
}

body:json {
  {
    "@context": [
      "https://w3id.org/edc/connector/management/v2"
    ],
    "@type": "CatalogRequest",
    "counterPartyAddress": "{{PROVIDER_DSP}}",
    "counterPartyId": "{{PROVIDER_ID}}",
    "protocol": "dataspace-protocol-http:2025-1",
    "querySpec": {
      "offset": 0,
      "limit": 50
    }
  }
}

script:post-response {
  test("Status code is >=200 and <300", function () {
      expect(res.getStatus() < 300 && res.getStatus() >= 200).to.be.true

      const assetId = bru.getVar("C2P_ASSET_ID") || "asset-3";
      bru.setVar("C2P_ASSET_ID", assetId);
      const datasets = res.getBody()["dataset"] || [];
      const asset = datasets.find((ds) => ds["@id"] == assetId);
      if (!asset) {
          throw new Error(`${assetId} was not found in provider catalog`);
      }

      const offer = asset["hasPolicy"][0];
      const rule = (offer["obligation"] && offer["obligation"][0]) || (offer["permission"] && offer["permission"][0]);
      const constraints = rule ? rule["constraint"] : [];
      const constraint = Array.isArray(constraints) ? constraints[0] : constraints;

      bru.setVar("C2P_POLICY_ID", offer["@id"]);
      bru.setVar("C2P_POLICY_LEFT_OPERAND", constraint["leftOperand"]);
      bru.setVar("C2P_POLICY_RIGHT_OPERAND", constraint["rightOperand"]);
      bru.setVar("C2P_CONTRACT_NEGOTIATION_ID", null);
      bru.setVar("C2P_CONTRACT_AGREEMENT_ID", null);
      bru.setVar("C2P_TRANSFER_PROCESS_ID", null);
      bru.setVar("C2P_AUTHORIZATION", null);
      bru.setVar("C2P_DOWNLOAD_URL", null);
      console.log("C2P asset id", assetId);
      console.log("C2P policy id", bru.getVar("C2P_POLICY_ID"));
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
